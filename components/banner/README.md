# Banner Generator - Refactored Structure

## 📁 Новая структура компонентов

```
components/banner/
├── README.md (этот файл)
├── index.ts (главный экспорт)
│
├── types/
│   └── index.ts (TypeScript типы)
│
├── utils/
│   └── imageDB.ts (IndexedDB класс)
│
├── hooks/
│   ├── useImageDB.ts (хук для ImageDB)
│   └── useLocalStorage.ts (хук для localStorage)
│
├── top-bar/
│   ├── index.ts
│   ├── TopBar.tsx (главный компонент топ-бара)
│   ├── DeviceSelector.tsx (переключатель iPhone/iPad)
│   └── ExportAllButton.tsx (кнопка экспорта)
│
├── preview-panel/
│   └── (TODO: компоненты превью)
│
├── settings-panel/
│   ├── NumberInputWithSlider.tsx (слайдер с числом)
│   └── (TODO: остальные настройки)
│
└── dialogs/
    └── (TODO: диалоги)
```

## 🎯 Преимущества новой структуры

### 1. **Модульность**
- Каждый компонент в отдельном файле
- Легко найти и редактировать нужный компонент
- Переиспользование компонентов

### 2. **Типизация**
- Все типы в одном месте (`types/index.ts`)
- TypeScript автодополнение работает идеально
- Меньше ошибок во время разработки

### 3. **Управление состоянием**
- Кастомные хуки инкапсулируют логику
- useImageDB - работа с базой данных
- useLocalStorage - работа с хранилищем

### 4. **Тестирование**
- Каждый компонент можно тестировать отдельно
- Легко писать unit-тесты
- Изолированная логика

### 5. **Масштабируемость**
- Добавление новых фич = новый компонент
- Не трогаем существующий код
- Легко работать в команде

## 📝 Как использовать

### Импорт компонентов

```tsx
// Импорт из главного индекса
import { TopBar, DeviceSelector, useImageDB } from '@/components/banner';

// Или напрямую
import { TopBar } from '@/components/banner/top-bar';
```

### Пример использования

```tsx
import { useState } from 'react';
import { TopBar, useImageDB, type DeviceType } from '@/components/banner';
import { LANGUAGES } from '@/lib/constants';

export default function MyComponent() {
  const [device, setDevice] = useState<DeviceType>('iphone');
  const [language, setLanguage] = useState('en');
  const imageDB = useImageDB();

  return (
    <TopBar
      selectedDevice={device}
      onDeviceChange={setDevice}
      languages={LANGUAGES}
      activeLanguage={language}
      onLanguageChange={setLanguage}
      isExporting={false}
      exportProgress={0}
      onExportAll={() => {}}
    />
  );
}
```

## 🚀 Следующие шаги

1. ✅ Создана базовая структура
2. ✅ Вынесен ImageDB
3. ✅ Созданы типы
4. ✅ Созданы базовые хуки
5. ✅ Созданы компоненты TopBar
6. 🔄 TODO: Создать компоненты PreviewPanel
7. 🔄 TODO: Создать компоненты SettingsPanel
8. 🔄 TODO: Создать компоненты Dialogs
9. 🔄 TODO: Рефакторить главный BannerGenerator
10. 🔄 TODO: Написать тесты

## 💡 Советы по разработке

- **Добавление нового компонента**: Создайте файл в соответствующей папке, экспортируйте через index.ts
- **Добавление типа**: Добавьте в `types/index.ts`
- **Добавление утилиты**: Поместите в `utils/`
- **Добавление хука**: Поместите в `hooks/`

## 📚 Ресурсы

- [React Best Practices](https://react.dev/learn)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)
- [Component Patterns](https://www.patterns.dev/)
