# 🚨 Troubleshooting Guide: Как мы восстановили рабочую версию

## 📅 Дата инцидента: 6 ноября 2025

## 🔴 Проблема

На https://yurabruskov.github.io/App_screen/ появилась "древняя версия" вместо новой рабочей версии с:
- Device selector (iPhone/iPad)
- Черным топ баром (#1E1E1E, #2C2C2C)
- iOS-style дизайном
- Custom styles

---

## 🔍 Причина проблемы

### Что случилось:

1. **Pull Request #39** из ветки `claude/add-refactoring-plan-011CUpq5zrgBD5W8qSZCUoVV` был влит в ветку **gh-pages**

2. **НО!** Эта ветка была создана из ветки **source-code**, которая содержит:
   - ❌ TypeScript файлы (`app/layout.tsx`, `app/page.tsx`)
   - ❌ Исходники компонентов (`components/banner-generator.tsx`)
   - ❌ Конфигурационные файлы (`package.json`, `tsconfig.json`)
   - ❌ `REFACTORING_PLAN.md` (документация для разработки)

3. **Результат**: В ветку `gh-pages` попали **исходники**, а не **собранная статика**!

### Техническая детализация:

```bash
# История коммитов gh-pages:
d3e2c05 deploy: Merge pull request #39  ← ПРОБЛЕМА ЗДЕСЬ!
51cd1c3 Merge pull request #39
b552c66 docs: Add comprehensive refactoring plan  ← Из source-code!
5e3bcac deploy: Merge pull request #38
b2689cf Merge pull request #38
f7f5b76 restore: Bring back working version  ← РАБОЧАЯ ВЕРСИЯ!
```

**Что попало в gh-pages после bad merge:**
```
gh-pages (НЕПРАВИЛЬНО):
├── app/
│   ├── layout.tsx  ❌ TypeScript!
│   └── page.tsx    ❌ TypeScript!
├── components/
│   └── banner-generator.tsx  ❌ 70KB исходников!
├── lib/
├── _next/static/chunks/app/
│   └── page-17df6877ead5e2da.js  ❌ Неправильный build!
├── package.json  ❌
└── REFACTORING_PLAN.md  ❌
```

**Что ДОЛЖНО быть в gh-pages:**
```
gh-pages (ПРАВИЛЬНО):
├── _next/static/chunks/app/
│   └── page-569d2104951887d3.js  ✅ Рабочий минифицированный JS!
├── index.html  ✅ Статический HTML
├── custom-styles.css  ✅ CSS стили
└── (только готовые к публикации файлы)
```

---

## ✅ Решение

### Шаг 1: Идентификация рабочей версии

Нашли commit **f7f5b76** "restore: Bring back working version", который содержал правильную статику.

### Шаг 2: Создание исправляющей ветки

```bash
# Checkout последней версии gh-pages (испорченной)
git checkout origin/gh-pages
git checkout -b claude/fix-gh-pages-v3-011CUpq5zrgBD5W8qSZCUoVV

# Удаление исходников
git rm -rf app/ lib/ components.json next-env.d.ts REFACTORING_PLAN.md

# Восстановление правильных файлов из f7f5b76
git checkout f7f5b76 -- index.html _next/ custom-styles.css components/

# Удаление старого неправильного JS файла
git rm _next/static/chunks/app/page-17df6877ead5e2da.js

# Коммит
git commit -m "fix: Revert gh-pages to working static version"
git push
```

### Шаг 3: Создание и merge Pull Request

**PR #40**: https://github.com/yurabruskov/App_screen/pull/40

После merge сайт заработал правильно! ✅

---

## 🎯 Почему GitHub Actions показывал ошибку, но сайт работал?

В ветке `gh-pages` был файл `.github/workflows/deploy.yml`, который при каждом push пытался:

```yaml
- npm ci              # ❌ Нет package.json в gh-pages!
- npm run build       # ❌ Нет исходников!
- Deploy из ./out     # ❌ Нет этой папки!
```

Поэтому workflow падал с `exit code 1`.

**НО!** GitHub Pages использует **встроенный механизм** `pages-build-deployment`, который:
- ✅ Берет статические файлы из корня ветки gh-pages
- ✅ Публикует их как есть
- ✅ Не требует npm, build, etc.

Поэтому сайт работал несмотря на ошибку в кастомном workflow! 🎉

---

## 📚 Структура репозитория

У нас **два типа веток** с разным назначением:

### 🔧 Ветка `source-code` - РАЗРАБОТКА

**Содержимое:**
```
source-code/
├── app/
│   ├── layout.tsx
│   └── page.tsx
├── components/
│   ├── banner-generator.tsx  (1905 строк исходников)
│   ├── ui/
│   └── banner/
├── lib/
├── hooks/
├── package.json
├── tsconfig.json
├── next.config.mjs
└── REFACTORING_PLAN.md
```

**Для чего:**
- ✅ Редактирование кода
- ✅ Добавление новых фич
- ✅ Рефакторинг
- ✅ Разработка

**Команды:**
```bash
npm install
npm run dev     # Локальный сервер
npm run build   # Сборка в папку out/
```

---

### 🌐 Ветка `gh-pages` - ПРОДАКШЕН

**Содержимое:**
```
gh-pages/
├── _next/
│   └── static/
│       ├── chunks/
│       │   └── app/
│       │       └── page-569d2104951887d3.js  (минифицированный)
│       └── css/
├── index.html
├── custom-styles.css
├── 404.html
└── (только статические файлы)
```

**Для чего:**
- ✅ Публикация на https://yurabruskov.github.io/App_screen/
- ✅ Только готовые к показу файлы
- ✅ Минифицированный код

**Важно:**
- ❌ **НИКОГДА** не добавлять исходники (*.tsx, *.ts)
- ❌ **НИКОГДА** не добавлять package.json
- ✅ **ТОЛЬКО** результат `npm run build`

---

## 🚀 Правильный workflow разработки

### Вариант 1: Ручной деплой (текущий)

```bash
# 1. Работа с исходниками
git checkout source-code
git pull origin source-code

# Редактируете файлы в components/, app/, etc.
# ...

# 2. Сборка проекта
npm run build
# Создается папка out/ с готовыми файлами

# 3. Деплой в gh-pages
git checkout gh-pages
git pull origin gh-pages

# Копируем ТОЛЬКО содержимое out/
cp -r out/* .

# Проверяем что скопировалось
ls -la  # Должны быть _next/, index.html, etc.

# Коммитим
git add .
git commit -m "deploy: Update from source-code"

# Создаем ветку для PR
git checkout -b claude/deploy-updates-[SESSION_ID]
git push -u origin claude/deploy-updates-[SESSION_ID]

# 4. Создаем PR в gh-pages
# После merge → сайт автоматически обновится через 1-2 минуты
```

### Вариант 2: Автоматический CI/CD (рекомендуется)

**Можно настроить GitHub Actions** в ветке `source-code`, который:

1. При push в `source-code`
2. Автоматически запускает `npm run build`
3. Автоматически коммитит результат в `gh-pages`
4. Сайт обновляется автоматически

**Пример workflow** (для `source-code/.github/workflows/deploy.yml`):

```yaml
name: Build and Deploy to gh-pages

on:
  push:
    branches: [source-code]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install and Build
        run: |
          npm ci
          npm run build

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./out
          publish_branch: gh-pages
```

---

## ⚠️ Правила, чтобы избежать проблем в будущем

### ✅ DO (Делать):

1. **В source-code:**
   - Разрабатывать код
   - Хранить исходники
   - Делать рефакторинг
   - Хранить документацию для разработки

2. **В gh-pages:**
   - Только результат `npm run build`
   - Только статические файлы
   - Можно редактировать `custom-styles.css` для быстрых CSS правок

3. **При создании PR:**
   - Проверять, из какой ветки в какую мерджите
   - Source code → source-code ✅
   - Build results → gh-pages ✅
   - Source code → gh-pages ❌ **ОПАСНО!**

### ❌ DON'T (Не делать):

1. **НЕ мерджить** ветки из `source-code` напрямую в `gh-pages`
2. **НЕ добавлять** в `gh-pages`:
   - TypeScript файлы (*.tsx, *.ts)
   - package.json
   - node_modules/
   - Исходники компонентов
   - Документацию для разработки (например, REFACTORING_PLAN.md принадлежит source-code)

3. **НЕ пытаться** запустить `npm install` или `npm run build` в ветке `gh-pages` - там нет исходников!

---

## 🔧 Чеклист перед деплоем

Перед созданием PR в `gh-pages`, убедитесь:

```bash
# Находимся в gh-pages
git checkout gh-pages

# Проверяем содержимое
ls -la

# Должны быть:
✅ _next/
✅ index.html
✅ custom-styles.css
✅ 404.html
✅ .nojekyll

# НЕ должно быть:
❌ app/
❌ components/ (кроме components/banner/ с минифицированными файлами если есть)
❌ lib/
❌ package.json
❌ tsconfig.json
❌ node_modules/
❌ *.tsx файлов
❌ *.ts файлов

# Проверяем что index.html ссылается на правильный JS
grep "page-" index.html
# Должен быть: page-569d2104951887d3.js или похожий хеш

# Проверяем что JS файл существует
ls -lh _next/static/chunks/app/page-*.js
# Должен быть 1 файл размером ~80KB
```

---

## 📖 Полезные команды для диагностики

```bash
# Посмотреть историю коммитов
git log --oneline -20

# Посмотреть что изменилось в коммите
git show <commit-hash> --stat

# Найти рабочую версию в истории
git log --all --grep="working version"

# Посмотреть файлы из конкретного коммита
git show <commit>:index.html

# Сравнить ветки
git diff gh-pages source-code --stat

# Проверить размер JavaScript файла (должен быть минифицированный)
wc -l _next/static/chunks/app/page-*.js
# Результат: 1-5 строк = минифицированный ✅
# Результат: 1000+ строк = исходники ❌
```

---

## 🎉 Итог

### Что мы узнали:

1. **source-code** и **gh-pages** - это два разных мира
2. В gh-pages должна быть только статика
3. Merge из source-code в gh-pages = 💣 катастрофа
4. Всегда проверяйте направление PR!

### Что исправили:

- ✅ Восстановили рабочую версию с device selector
- ✅ Удалили исходники из gh-pages
- ✅ Сайт работает правильно
- ✅ Создали этот документ для будущего

### Контакты для вопросов:

Если что-то сломалось снова:
1. Проверьте этот документ
2. Убедитесь что в gh-pages только статика
3. Найдите последний рабочий коммит: `git log --grep="working"`
4. Восстановите из него файлы

---

**Дата создания:** 6 ноября 2025
**Последнее обновление:** 6 ноября 2025
**Статус:** ✅ Проблема решена, сайт работает
